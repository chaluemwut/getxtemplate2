import 'package:flutter/material.dart';


class AddGradeWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('Add grade')],
    );
  }
}
